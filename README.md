# local-BLAST-capture_seq_and_ID

#making a local blast with BLAST+ and taking the sequencies that matched into a new separete file with the ID(query and subject) and the sequence(query and subject)

